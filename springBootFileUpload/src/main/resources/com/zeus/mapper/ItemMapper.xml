<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeus.mapper.ItemMapper">
	<insert id="create">
		INSERT INTO item
		(id,name,price,description,url)
		VALUES(item_seq.NEXTVAL,#{name},#{price},#{description},#{url})
	</insert>
	<select id="read" resultType="Item">
		SELECT
		id,name,price,description,url
		FROM item
		WHERE id = #{id}
	</select>
	<update id="update">
		UPDATE item
		SET name = #{name},
		price = #{price},
		description = #{description},
		url = #{url}
		WHERE id = #{id}
	</update>
	<delete id="delete">

	</delete>
	<select id="list" resultType="Item">
		<!-- 오라클에서 쓰는 대소 비교표현이 헷갈릴 수가 있어서 ![CDATA[]]를 써준다. 태그로 인식하게 하지 않기위해 -->
<![CDATA[ 
SELECT id, name, price, description, url FROM item 
WHERE id > 0  
ORDER BY id DESC 
]]>
	</select>
	<select id="getPicture" resultType="String">
		SELECT url FROM item WHERE id
		= #{id}
	</select>
</mapper>